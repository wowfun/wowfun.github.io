---
publish: true
title: CJK Showcase
nav_order: 60
aliases:
  - 中日韩示例
tags:
  - guide/cjk
  - 中文
description: Mixed Chinese, Japanese, Latin, and code samples for typography and search.
created: 2026-07-31
updated: 2026-07-31
---

# CJK Showcase

中文、日文和拉丁字母可以写在同一个知识库里。正文优先使用系统中的 Noto Serif CJK 或思源宋体，界面则优先使用 Noto Sans CJK 或思源黑体。

## 中文检索

搜索索引会为连续的 CJK 文本生成稳定的单字和双字 token。例如，“知识花园”会产生“知”“识”“知识”“识花”等索引项。索引与查询使用同一套确定性分词，不依赖浏览器版本。

链接仍然使用原始标题与路径：[[docs/development/architecture|编译器架构]]、[[docs/Getting Started|开始使用]]。

> [!tip] 中英文混排
> Keep product names and code in their original form. 中文说明与 `website.source` 这样的代码可以自然共存。

## 日本語

日本語の見出しと本文も同じ検索データに入ります。ひらがな、カタカナ、漢字を含む短い語でも、決定的な CJK token が最低限の検索結果を支えます。

## Code and math

Code uses the platform monospace stack, so no separate font download is required:

```ruby
request = BuildRequest.new(snapshot:, config:)
result = VaultCompiler.compile(request)
```

混合脚本的数学公式也保留原始文本：$\alpha + \beta = \gamma$。

返回 [[index|首页]]，或继续阅读 [[Customization|自定义主题]]。
